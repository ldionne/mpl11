/**
 * This file is just required so the executable can link, because
 * all unit tests are compile-time anyway.
 */

int main(int argc, char const* argv[]) {
    (void)argc, (void)argv;
    return 0;
}
